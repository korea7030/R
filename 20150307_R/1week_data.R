# ????Ȯ?? : ?۾??????丮 ??�� ?? ?۾? ?????丮???? 
#            ?ǽ��? ?????? ?ٿ??ε? Ȯ?? ?ʿ?

getwd()

setwd("?۾??????丮?? ???ڰ? ?Է?")

load("dmhit.RData")

dim(dmhit)
head(dmhit)
summary(dmhit)
str(dmhit)

dmhittest <- complete.cases(dmhit)
dim(dmhittest)
length(dmhittest)

a <- dmhit[dmhit$DM == "????" & dmhittest, "??????"]
mean(a)
hist(a)
hist(a, breaks = 30)

b <- dmhit[dmhit$DM == "?̼???" & dmhittest, "??????"]
mean(b)
hist(b)
hist(b,breaks = 30)

t.test(a,b)

(tresults <- t.test(a, b))

names(tresults)

tresults$null.value

tresults$parameter

tresults$statistic

tresults$estimate

tresults$alternative

tresults$p.value

tresults$data.name



