library(MVA)
app <- read.table(file="Applicant.TXT")
app <- read.table(file="Applicant.TXT", header=T)
head(app)
str(app)
app <= app[,-1]
app <- app[,-1]
str(app)
fal <- factanal(app)
fal <- factanal(app, 3)
fal
fal <- factanal(app, 4)
fal
print(fal, digits=2)
print(fal, digits=2, sort=T)
